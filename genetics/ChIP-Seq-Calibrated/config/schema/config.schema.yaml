$schema: "http://json-schema.org/draft-04/schema#"

_yamldoc_title: Parameter Reference
_yamldoc_description: Parameters available for the calibrated ChIP-seq workflow.

definitions:
  bool_or_legacy_bool:
    anyOf:
      - type: boolean
      - type: string
        enum: ["True", "False", "true", "false"]

type: object

properties:
  analysis_name:
    type: string

  fastq_home_dir:
    type: string

  sample_sheet:
    type: string

  single_paired_end:
    type: string
    enum: [single, paired]

  cutadapters_bool:
    $ref: "#/definitions/bool_or_legacy_bool"

  bowtie2_dir:
    type: string

  genome:
    type: string
    enum: [hg19, hg38, mm9, mm10, mm39]

  spikein:
    type: string
    enum: [hg19, hg38, mm9, mm10, mm39]

  input_provided:
    $ref: "#/definitions/bool_or_legacy_bool"

  bigDataUrl:
    type: string

  move_output_files:
    $ref: "#/definitions/bool_or_legacy_bool"

  output_dir:
    type: string

  tag_output_dir:
    type: string

  move_bw_public_folder:
    $ref: "#/definitions/bool_or_legacy_bool"

  public_output_dir:
    type: string

  tag_public_output_dir:
    type: string

  trimming_SE_extra:
    type: string

  trimming_PE_extra:
    type: string

  bowtie2_extra:
    type: string

  samtools_filtering_extra:
    type: string

  samtools_sort_extra:
    type: string

  mark_duplicates_extra:
    type: string

  samtools_index_extra:
    type: string

  bam_coverage_params1:
    type: string

  bam_coverage_params2:
    type: string

  delete_intermediate_files:
    $ref: "#/definitions/bool_or_legacy_bool"

  single_paired_folder_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  reads_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  trimming_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  bowtie2_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  sorted_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  duplicates_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  splitgenome_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  readcounts_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  downsampling_factor_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  downsampling_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  bam_coverage_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  peaks_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  track_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  single_paired_folder:
    type: string

  reads:
    type: string

  reads_qc:
    type: string

  trimming:
    type: string

  trimming_qc:
    type: string

  bowtie2:
    type: string

  bowtie2_qc:
    type: string

  sorted:
    type: string

  duplicates:
    type: string

  duplicates_qc:
    type: string

  splitgenome:
    type: string

  readcounts:
    type: string

  readcounts_tmp:
    type: string

  downsampling_factor:
    type: string

  downsampling_factor_log:
    type: string

  downsampling:
    type: string

  downsampling_log:
    type: string

  bam_coverage:
    type: string

  peaks:
    type: string

  peaks_log:
    type: string

  track:
    type: string

  cleaning:
    type: string
