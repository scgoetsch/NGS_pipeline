$schema: "http://json-schema.org/draft-04/schema#"

_yamldoc_title: Parameter Reference
_yamldoc_description: Parameters available for the CATCH-UP workflow.

definitions:
  bool_or_legacy_bool:
    anyOf:
      - type: boolean
      - type: string
        enum: ["True", "False", "true", "false"]

type: object

properties:
  analysis_name:
    type: string

  fastq_home_dir:
    type: string

  sample_sheet:
    type: string

  single_paired_end:
    type: string
    enum: [single, paired]

  concatenate_fastq:
    $ref: "#/definitions/bool_or_legacy_bool"

  cutadapters_bool:
    $ref: "#/definitions/bool_or_legacy_bool"

  aligner_algorithm:
    type: string
    enum: [bowtie2, bwa-mem2]

  aligner_dir:
    type: string

  genome_built_version:
    type: string
    enum: [hg19, hg38, mm9, mm10, mm39]

  merge_bams:
    $ref: "#/definitions/bool_or_legacy_bool"

  peak_caller:
    type: string
    enum: [lanceotron, macs2, macs3]

  bigDataUrl:
    type: string

  move_output_files:
    $ref: "#/definitions/bool_or_legacy_bool"

  output_dir:
    type: string

  tag_output_dir:
    type: string

  move_bw_public_folder:
    $ref: "#/definitions/bool_or_legacy_bool"

  public_output_dir:
    type: string

  tag_public_output_dir:
    type: string

  trimming_SE_extra:
    type: string

  trimming_PE_extra:
    type: string

  aligner_extra:
    type: string

  filtering_extra:
    type: string

  samtools_sort_extra:
    type: string

  mark_duplicates_extra:
    type: string

  samtools_index_extra:
    type: string

  bam_coverage_params1:
    type: string

  bam_coverage_params2:
    type: string

  delete_intermediate_files:
    $ref: "#/definitions/bool_or_legacy_bool"

  single_paired_folder_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  reads_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  trimming_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  aligner_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  filtering_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  sorted_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  duplicates_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  merge_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  bam_coverage_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  peaks_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  track_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  single_paired_folder:
    type: string

  reads:
    type: string

  reads_qc:
    type: string

  reads_concat:
    type: string

  reads_concat_qc:
    type: string

  trimming:
    type: string

  trimming_qc:
    type: string

  aligner:
    type: string

  aligner_qc:
    type: string

  filtering:
    type: string

  sorted:
    type: string

  duplicates:
    type: string

  duplicates_qc:
    type: string

  merge:
    type: string

  merge_qc:
    type: string

  bam_coverage:
    type: string

  peaks:
    type: string

  peaks_log:
    type: string

  track:
    type: string

  cleaning:
    type: string
