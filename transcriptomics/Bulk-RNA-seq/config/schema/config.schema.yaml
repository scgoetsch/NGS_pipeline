$schema: "http://json-schema.org/draft-04/schema#"

_yamldoc_title: Parameter Reference
_yamldoc_description: Parameters available for the bulk RNA-seq workflow.

definitions:
  bool_or_legacy_bool:
    anyOf:
      - type: boolean
      - type: string
        enum: ["True", "False", "true", "false"]

type: object

properties:
  analysis_name:
    type: string

  fastq_home_dir:
    type: string

  sample_sheet:
    type: string

  single_paired_end:
    type: string
    enum: [single, paired]

  concatenate_fastq:
    $ref: "#/definitions/bool_or_legacy_bool"

  cutadapters_bool:
    $ref: "#/definitions/bool_or_legacy_bool"

  star_dir:
    type: string

  gtf_annotation:
    type: string

  genome_built_version:
    type: string
    enum: [hg19, hg38, mm9, mm10, mm39]

  merge_bams:
    $ref: "#/definitions/bool_or_legacy_bool"

  bigDataUrl:
    type: string

  move_output_files:
    $ref: "#/definitions/bool_or_legacy_bool"

  output_dir:
    type: string

  tag_output_dir:
    type: string

  move_bw_public_folder:
    $ref: "#/definitions/bool_or_legacy_bool"

  public_output_dir:
    type: string

  tag_public_output_dir:
    type: string

  trimming_SE_extra:
    type: string

  trimming_PE_extra:
    type: string

  samtools_sort_extra:
    type: string

  mark_duplicates_extra:
    type: string

  samtools_index_extra:
    type: string

  bam_coverage_params1:
    type: string

  bam_coverage_params2:
    type: string

  delete_intermediate_files:
    $ref: "#/definitions/bool_or_legacy_bool"

  single_paired_folder_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  reads_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  trimming_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  star_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  sorted_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  duplicates_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  merge_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  indices_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  bam_coverage_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  feature_counts_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  track_rm:
    $ref: "#/definitions/bool_or_legacy_bool"

  tmp:
    type: string

  single_paired_folder:
    type: string

  reads:
    type: string

  reads_qc:
    type: string

  reads_concat:
    type: string

  reads_concat_qc:
    type: string

  trimming:
    type: string

  trimming_qc:
    type: string

  star:
    type: string

  star_qc:
    type: string

  sorted:
    type: string

  duplicates:
    type: string

  duplicates_qc:
    type: string

  merge:
    type: string

  merge_qc:
    type: string

  indices:
    type: string

  bam_coverage:
    type: string

  feature_counts:
    type: string

  track:
    type: string

  cleaning:
    type: string
